/* 
scrivere una funzione askNumber(), di tipo intero, con due parametri interi, l e r (left, right);

la funzione chiede all'utente un numero n compreso tra l e r (l <= n <= r) e lo ritorna al chiamante (return n;)

se l'utente non inserisce un numero adatto, la funzione continua a chiederlo (while / do )

il programma main fa il test della funzione (chiama la funzione per testarla)
*/

#include <stdio.h>

int AskNumber(int L, int R){
    
} 



int main(){

}